import React from 'react'

const MyArea = () => {
  return (
    <div>
        <h1> MyArea </h1>
        <button onClick={() => window.location = '/form'}>Create Analysis</button>

    </div>
  )
}

export default MyArea